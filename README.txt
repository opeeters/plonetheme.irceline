Introduction
============

An installable theme for Plone 4 - www.irceline.be

This theme is based on the default Plone 4 theme "Sunburst".
Included is a random image changer as navigation background. All .jpg images in 
a folder "/randomimages/" (at root-level) will be loaded at random. The image file need to be added as an object type "image".
